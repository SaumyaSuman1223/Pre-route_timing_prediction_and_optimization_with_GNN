# results/

- figures/          exported plots (correlation plots, two-stage vs. baseline comparison)
- metrics.csv        collected accuracy numbers across circuits/configs
- trained_models/    model checkpoints (gitignored by default -- use Git LFS if you
                      want to version them, otherwise keep this reproducible via
                      src/train.py instead)
