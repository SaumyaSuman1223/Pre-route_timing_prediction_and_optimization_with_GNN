# learning/

Mini-projects 1-8 from the learn-by-building curriculum. Each folder is self-contained
and runnable on its own (toy/synthetic data, no dependency on the real benchmark data).
Kept permanently — this is the visible record of how each real-pipeline component was
proven out before being promoted into src/.
