# 08 — Toy CNN on a synthetic grid (optional)

Only needed if attempting the CNN routing-resource stretch goal from the main workflow.
Safe to skip if staying with the GNN-only core scope.

Files:
- synthetic_grid_gen.py
- cnn_toy.py
