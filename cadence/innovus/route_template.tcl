# Continues from place_template.tcl's placed design — runs full routing, then extracts
# real parasitics and writes a POST-ROUTE timing report.
# report_timing > $OUT_DIR/${CIRCUIT}_${CONFIG}_postroute.rpt
