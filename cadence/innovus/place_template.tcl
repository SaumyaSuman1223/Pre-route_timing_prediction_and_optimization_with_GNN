# Parametrized Innovus placement script — reads a synthesized netlist, runs placement
# ONLY (no routing), and writes a post-placement (PRE-ROUTE) timing report.
# report_timing > $OUT_DIR/${CIRCUIT}_${CONFIG}_preroute.rpt
